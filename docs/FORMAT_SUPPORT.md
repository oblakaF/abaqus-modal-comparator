# Supported source data

## Abaqus

- `.odb` read by the installed Abaqus Python interpreter
- extracted `manifest.json` cache

## Simcenter Testlab

- `.unv` / `.uff` geometry and modal-vector datasets
- `.lms` only when a companion universal file is available beside the project
